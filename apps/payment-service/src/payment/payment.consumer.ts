import {Injectable,OnModuleInit} from '@nestjs/common';import {RabbitService} from './rabbit.service';import {PaymentService} from './payment.service';import {BankEvent} from '../common/bank-event';
@Injectable() export class PaymentConsumer implements OnModuleInit{constructor(private readonly rabbit:RabbitService,private readonly service:PaymentService){}async onModuleInit(){await this.rabbit.subscribe(async m=>{const e=JSON.parse(m.content.toString('utf8')) as BankEvent;if(e.eventType!=='account.funds.reserved'||!e.eventId||!e.correlationId)throw new Error('Invalid event');await this.service.handle(e);});}}
