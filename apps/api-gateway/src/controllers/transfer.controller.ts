import {BadRequestException,Body,Controller,ForbiddenException,Get,HttpCode,Param,Post,Req} from '@nestjs/common';import {randomUUID} from 'crypto';import {ConfigService} from '@nestjs/config';import {RabbitService} from '../rabbit/rabbit.service';import {Roles} from '../auth/roles.decorator';import {proxyJson} from './proxy.util';import {currentCorrelationId} from '../correlation/correlation';
@Controller('api/transfers') export class TransferController{constructor(private readonly rabbit:RabbitService,private readonly c:ConfigService){}
 @Roles('CLIENT') @Post() @HttpCode(202) async create(@Req()req:any,@Body()b:{sourceAccount:string;targetAccount:string;amount:number}){if(!b.sourceAccount||!b.targetAccount||!Number.isFinite(Number(b.amount))||Number(b.amount)<=0)throw new BadRequestException('sourceAccount, targetAccount and positive amount are required');const source=await proxyJson(this.c.get('ACCOUNT_SERVICE_URL','http://localhost:3004'),`/api/accounts/${b.sourceAccount}`,'GET');if(source.customerId!==req.user.customerId)throw new ForbiddenException('Source account does not belong to authenticated customer');// La Saga usa el correlationId del request: una sola traza de punta a punta. Reenviar
 // el mismo X-Correlation-Id es un reintento: Transaction no crea una segunda transferencia.
 const correlationId=currentCorrelationId()??randomUUID();const e=await this.rabbit.publish('transaction.transfer.requested',correlationId,{sourceAccount:b.sourceAccount,targetAccount:b.targetAccount,amount:Number(b.amount),requestedBy:req.user.customerId,requestedEmail:req.user.email});return{accepted:true,correlationId,eventId:e.eventId,status:'PENDING'};}
 // Un CLIENT solo ve el estado de transferencias que salen de una cuenta suya (misma regla que el historial).
 @Get(':correlationId') async status(@Req()req:any,@Param('correlationId')id:string){const t=await proxyJson(this.c.get('TRANSACTION_SERVICE_URL','http://localhost:3003'),`/api/transactions/correlation/${encodeURIComponent(id)}`,'GET');if(req.user?.role==='CLIENT'){const a=await proxyJson(this.c.get('ACCOUNT_SERVICE_URL','http://localhost:3004'),`/api/accounts/${encodeURIComponent(t.sourceAccount)}`,'GET');if(a.customerId!==req.user.customerId)throw new ForbiddenException('Transfer does not belong to authenticated customer');}return t;}}
