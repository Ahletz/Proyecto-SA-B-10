import {Injectable,OnModuleDestroy} from '@nestjs/common';import {ConfigService} from '@nestjs/config';import * as amqp from 'amqplib';import type {Channel,ChannelModel} from 'amqplib';import {randomUUID} from 'crypto';
@Injectable() export class RabbitService implements OnModuleDestroy{private conn:ChannelModel|null=null;private channel:Channel|null=null;constructor(private readonly c:ConfigService){}private async ch(){if(this.channel)return this.channel;this.conn=await amqp.connect(this.c.getOrThrow<string>('RABBITMQ_URL'));this.channel=await this.conn.createChannel();await this.channel.assertExchange('bank.events','topic',{durable:true});return this.channel;}async publish(eventType:string,correlationId:string,payload:any){const ch=await this.ch();const event={eventId:randomUUID(),eventType,version:1,timestamp:new Date().toISOString(),correlationId,payload};ch.publish('bank.events',eventType,Buffer.from(JSON.stringify(event)),{persistent:true,contentType:'application/json',messageId:event.eventId,correlationId});return event;}async onModuleDestroy(){if(this.channel)await this.channel.close();if(this.conn)await this.conn.close();}}
