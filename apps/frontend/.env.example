# Frontend Environment Variables
# Copy to .env.local and adjust as needed

# API Gateway URL (optional, uses proxy by default in dev)
# VITE_API_URL=http://localhost:8080

# Enable debug mode
# VITE_DEBUG=true
