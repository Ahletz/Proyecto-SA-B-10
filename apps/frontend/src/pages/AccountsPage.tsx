import {useEffect,useState} from 'react';import{api}from'../lib/api';import{useAuthStore}from'../store/authStore';
export function AccountsPage(){const{customer}=useAuthStore();const[a,setA]=useState<any[]>([]);const[type,setType]=useState('MONETARY');const[initial,setInitial]=useState(1000);const[customerId,setCustomerId]=useState('');const load=()=>api(`/api/accounts${customer?.role==='CLIENT'?'':customerId?`?customerId=${customerId}`:''}`).then(setA);useEffect(()=>{if(customer)load()},[customer]);return <section className="page"><h1>Cuentas</h1>{customer?.role!=='CLIENT'&&<input placeholder="customerId para consultar/crear" value={customerId} onChange={e=>setCustomerId(e.target.value)}/>}<div className="toolbar"><select value={type} onChange={e=>setType(e.target.value)}><option>MONETARY</option><option>SAVINGS</option></select><input type="number" value={initial} onChange={e=>setInitial(Number(e.target.value))}/><button onClick={async()=>{await api('/api/accounts',{method:'POST',body:JSON.stringify({type,initialBalance:initial,customerId:customerId||undefined})});load();}}>Crear cuenta</button></div><table><thead><tr><th>ID</th><th>Tipo</th><th>Saldo</th><th>Disponible</th><th>Estado</th></tr></thead><tbody>{a.map(x=><tr key={x.accountId}><td>{x.accountId}</td><td>{x.type}</td><td>Q{x.balance}</td><td>Q{x.availableBalance}</td><td>{x.status}</td></tr>)}</tbody></table></section>}
