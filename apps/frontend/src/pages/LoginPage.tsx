import {useState} from 'react';import{useNavigate,Link}from'react-router-dom';import{useAuthStore}from'../store/authStore';
export function LoginPage(){const[u,setU]=useState('');const[p,setP]=useState('');const{login,loading,error}=useAuthStore();const n=useNavigate();return <main className="center"><form className="card form" onSubmit={async e=>{e.preventDefault();await login(u,p);n('/');}}><h1>Bank USAC</h1><h2>Iniciar sesión</h2>{error&&<p className="error">{error}</p>}<label>Usuario<input value={u} onChange={e=>setU(e.target.value)} required/></label><label>Contraseña<input type="password" value={p} onChange={e=>setP(e.target.value)} required/></label><button disabled={loading}>Ingresar</button><Link to="/register">Crear usuario</Link></form></main>}
