name: Integrante 2 CI

# Account Service y Payment Service (NestJS, lo que se despliega). El código
# Java paralelo de esas carpetas (pom.xml) no se compila aquí.

on:
  push:
    branches:
      - 'feature/**'
    paths:
      - 'apps/account-service/**'
      - 'apps/payment-service/**'
      - 'infrastructure/databases/account/**'
      - 'infrastructure/databases/payment/**'
      - '.github/workflows/integrante2-ci.yml'
      - '.github/workflows/reusable-node-ci.yml'

  pull_request:
    branches:
      - develop
    paths:
      - 'apps/account-service/**'
      - 'apps/payment-service/**'
      - 'infrastructure/databases/account/**'
      - 'infrastructure/databases/payment/**'
      - '.github/workflows/integrante2-ci.yml'
      - '.github/workflows/reusable-node-ci.yml'

permissions:
  contents: read

concurrency:
  group: integrante2-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  account-service:
    name: Account - Build and Test
    uses: ./.github/workflows/reusable-node-ci.yml
    with:
      service: account-service

  payment-service:
    name: Payment - Build and Test
    uses: ./.github/workflows/reusable-node-ci.yml
    with:
      service: payment-service
